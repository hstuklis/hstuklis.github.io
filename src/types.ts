export interface Config {
    site: string
    font: string
    title: string
    description: string
    author: string
    tags: string[]
    pageSize: number
    nav: string[]
    footerLinks: string[]
    rss: boolean
}